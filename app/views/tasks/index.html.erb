<p style="color: green"><%= notice %></p>

<h2>Tasks</h2>
<%= link_to "New task", new_task_path, class: 'my-3' %>
<table class="table">
  <tbody>
    <% @tasks.each do |task| %>
      <%= render task %>
    <% end %>
  </tbody>
</table>
